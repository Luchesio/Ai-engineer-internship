services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      DB_PATH: /data/chatbot.db
    volumes:
      - chatbot-data:/data
    restart: unless-stopped

volumes:
  chatbot-data: